var makePizza = {
    size : 16,
    shape : "round",
    flavour : "tikka",

    recipe : {
        step1 : [{
            makeDough : ["flour", "milk", "egg"]
        }],

        step2 : [{
            makeLayers : ["dough", "ketchup", "sausages", "olives", "cheese"]
        }],

        step3 : [{
            bake : ["Bake for 15 min"]
        }],
        step4 : function() {
            return "Serve it with garlic and peprika powder";
        }
    }
};
console.log(makePizza.shape);
